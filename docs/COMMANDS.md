# 📖 Полный список команд

## 📊 Информационные команды

### `/?`
Показывает полный статус системы:
- Состояние процесса (запущен/остановлен)
- Время запуска процесса
- Текущая температура
- Пороги MAX/MIN
- Девиация
- Состояние зуммера и клапана
- WiFi сигнал
- Время работы (uptime)

### `/help`
Показывает краткую справку по всем командам.

---

## 🎮 Управление процессом

### `/start`
**Запуск процесса автоматического контроля температуры.**

При запуске:
- Открывается клапан (D26 → LOW)
- Система начинает мониторинг температуры
- При превышении порога срабатывает защита

Логика работы:
1. Если `Температура >= MAX + Девиация`:
   - Закрыть клапан (D26 → HIGH)
   - Если разрешен зуммер → включить (D33 → HIGH)
   - Отправить уведомление

2. Если `Температура <= MAX`:
   - Открыть клапан (D26 → LOW)
   - Выключить зуммер (D33 → LOW)
   - Отправить уведомление

---

## 🔔 Управление зуммером

### `/alarm_on`
**Глобальное разрешение зуммера.**

- Устанавливает флаг `Alarm Enabled = ON`
- НЕ включает зуммер физически
- Зуммер будет включаться автоматически при превышении температуры

### `/alarm_off`
**Глобальный запрет зуммера.**

- Устанавливает флаг `Alarm Enabled = OFF`
- Выключает зуммер если он был включен (D33 → LOW)
- Зуммер не будет включаться автоматически

### `/zummer`
**Ручное переключение зуммера (триггер).**

- Работает **ТОЛЬКО** если `Alarm Enabled = ON`
- Переключает состояние D33: `HIGH ↔ LOW`
- Если зуммер запрещен → выдает ошибку

Пример использования:
```
/alarm_on     → Разрешить зуммер
/zummer       → Включить зуммер (D33 = HIGH)
/zummer       → Выключить зуммер (D33 = LOW)
/zummer       → Включить зуммер (D33 = HIGH)
/alarm_off    → Запретить и выключить зуммер
/zummer       → ОШИБКА: зуммер запрещен
```

---

## 🚰 Управление клапаном

### `/valve_on`
**Установить HIGH на D26 (закрыть клапан).**

- Прямое управление пином
- Работает независимо от процесса `/start`

### `/valve_off`
**Установить LOW на D26 (открыть клапан).**

- Прямое управление пином
- Работает независимо от процесса `/start`

---

## 🌡️ Настройка температуры

### `/temp_up`
**Установить порог MAX (максимальная температура).**

Диапазон: 0-100°C

Пример:
```
/temp_up
→ Введите порог MAX (0-100°C):
85
→ ✅ Порог MAX установлен: 85.0°C
```

### `/temp_down`
**Установить порог MIN (минимальная температура).**

Диапазон: 0-100°C

При падении ниже MIN → отправляется предупреждение.

### `/temp_current`
**Установить MAX равным текущей температуре.**

Быстрая установка порога без ручного ввода.

Пример:
```
Текущая температура: 78.5°C
/temp_current
→ ✅ Порог MAX установлен на текущую температуру: 78.5°C
```

### `/temp_dev`
**Установить девиацию (гистерезис).**

Диапазон: 0-5°C

Порог срабатывания = MAX + Девиация

Пример:
```
MAX = 85°C
Девиация = 2°C
Сработка при: 85 + 2 = 87°C
```

---

## ⏲️ Таймер

### `/set_timer`
**Запуск обратного отсчета.**

Диапазон: 1-9999 секунд

Таймер отображается на дисплее справа (4 цифры).

Пример:
```
/set_timer
→ Введите время таймера (1-9999 сек):
120
→ ⏲️ Таймер запущен на 120 сек.

[Через 120 секунд]
→ 🔔 ТАЙМЕР: Время истекло!
```

---

## 🔐 Сброс настроек

### Удержание кнопки BOOT (GPIO 0) - 5 секунд

- Удаляет все сохраненные настройки
- WiFi credentials
- Telegram токены
- Пороги температуры
- ESP32 перезагружается в режим AP для новой настройки

---

## 📊 Примеры использования

### Сценарий 1: Начало работы
```
/help                    → Список команд
/?                       → Проверить статус
/temp_up                 → Установить MAX = 90°C
/temp_dev                → Установить девиацию = 3°C
/alarm_on                → Разрешить зуммер
/start                   → Запустить процесс
```

### Сценарий 2: Быстрая настройка
```
/temp_current            → MAX = текущая температура
/temp_dev                → Девиация = 2°C
/alarm_on                → Разрешить звук
/start                   → Старт
```

### Сценарий 3: Ручное управление
```
/alarm_off               → Запретить зуммер
/valve_on                → Закрыть клапан вручную
/set_timer               → Таймер на 300 сек
[Через 5 минут]
/valve_off               → Открыть клапан
```

---

## ⚠️ Важные замечания

- Все настройки сохраняются в энергонезависимой памяти
- Процесс `/start` можно запустить только один раз (до перезагрузки)
- Команды `/valve_on` и `/valve_off` работают всегда, независимо от процесса
- Команда `/zummer` работает только если `/alarm_on`
