# ⚙️ Инструкция по настройке

## 📋 Содержание

1. [Установка Arduino IDE и библиотек](#1-установка-arduino-ide)
2. [Создание Telegram бота](#2-создание-telegram-бота)
3. [Получение Chat ID](#3-получение-chat-id)
4. [Первая загрузка кода](#4-первая-загрузка-кода)
5. [Web-настройка WiFi](#5-web-настройка-wifi)
6. [Проверка работы](#6-проверка-работы)
7. [Устранение проблем](#7-устранение-проблем)

---

## 1️⃣ Установка Arduino IDE

### Шаг 1: Скачать Arduino IDE

Скачайте последнюю версию (2.x): https://www.arduino.cc/en/software

### Шаг 2: Установить поддержку ESP32

1. Откройте Arduino IDE
2. `Файл → Настройки`
3. В поле "Дополнительные ссылки для Менеджера плат" добавьте:
```
   https://espressif.github.io/arduino-esp32/package_esp32_index.json
```
4. `Инструменты → Плата → Менеджер плат`
5. Найдите `esp32` by Espressif
6. Установите версию **3.0.0 или выше**

### Шаг 3: Установить библиотеки

`Скетч → Подключить библиотеку → Управление библиотеками`

Установите следующие библиотеки:

| Библиотека | Автор | Версия |
|------------|-------|--------|
| GyverDS18 | AlexGyver | Последняя |
| FastBot | AlexGyver | Последняя |
| ESPping | - | Последняя |
| GyverSegment | AlexGyver | Последняя |

**Встроенные библиотеки** (уже есть):
- WiFi
- WebServer
- Preferences

---

## 2️⃣ Создание Telegram бота

### Шаг 1: Открыть BotFather

1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/BotFather)
3. Нажмите `Start`

### Шаг 2: Создать бота

Отправьте команду:
```
/newbot
```

BotFather спросит:
```
Alright, a new bot. How are we going to call it? 
Please choose a name for your bot.
```

Введите имя (например):
```
My Samovar Bot
```

BotFather спросит username:
```
Good. Now let's choose a username for your bot. 
It must end in `bot`. Like this, for example: TetrisBot or tetris_bot.
```

Введите username (должен заканчиваться на `bot`):
```
my_samovar_bot
```

### Шаг 3: Получить токен

BotFather пришлет сообщение:
```
Done! Congratulations on your new bot. 
You will find it at t.me/my_samovar_bot. 
You can now add a description, about section and profile picture.

Use this token to access the HTTP API:
1234567890:ABCdefGHIjklMNOpqrsTUVwxyz123456789

Keep your token secure and store it safely, 
it can be used by anyone to control your bot.
```

**Скопируйте токен:**
```
1234567890:ABCdefGHIjklMNOpqrsTUVwxyz123456789
```

⚠️ **ВАЖНО:** Никому не показывайте этот токен!

### Шаг 4: Настроить бота (опционально)

Можно установить описание и аватар:
```
/setdescription
/setabouttext
/setuserpic
```

---

## 3️⃣ Получение Chat ID

### Метод 1: Через @userinfobot (рекомендуется)

1. Найдите [@userinfobot](https://t.me/userinfobot)
2. Нажмите `Start`
3. Бот пришлет ваш Chat ID:
```
   Id: 123456789
   First name: John
```

**Скопируйте число** (например, `123456789`)

### Метод 2: Через @getmyid_bot

1. Найдите [@getmyid_bot](https://t.me/getmyid_bot)
2. Нажмите `Start`
3. Скопируйте `Your user ID`

### Метод 3: Вручную через API

1. Напишите что-то своему боту (например, `/start`)
2. Откройте в браузере:
```
   https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates
```
   Замените `<YOUR_BOT_TOKEN>` на ваш токен
3. Найдите в JSON ответе поле `"id"` внутри `"from"`

---

## 4️⃣ Первая загрузка кода

### Шаг 1: Скачать проект
```bash
git clone https://github.com/your-username/samovar-esp32.git
cd samovar-esp32/SAMOVAR_V2
```

Или скачайте ZIP с GitHub и распакуйте.

### Шаг 2: Открыть проект

Откройте файл `SAMOVAR_V2.ino` в Arduino IDE

### Шаг 3: Выбрать плату

`Инструменты → Плата → esp32 → ESP32 Dev Module`

**Настройки платы:**
```
Board: "ESP32 Dev Module"
Upload Speed: "115200"
CPU Frequency: "240MHz"
Flash Frequency: "80MHz"
Flash Mode: "QIO"
Flash Size: "4MB (32Mb)"
Partition Scheme: "Default 4MB with spiffs"
Core Debug Level: "None"
```

### Шаг 4: Выбрать порт

`Инструменты → Порт → COMx` (Windows) или `/dev/ttyUSB0` (Linux)

**Не видите порт?** Установите драйвер:
- **CH340:** https://www.wch-ic.com/downloads/CH341SER_ZIP.html
- **CP2102:** https://www.silabs.com/developers/usb-to-uart-bridge-vcp-drivers

### Шаг 5: Загрузить код

1. Нажмите кнопку **Загрузить** (→)
2. Если ESP32 не входит в режим загрузки:
   - Удерживайте кнопку **BOOT**
   - Нажмите **Загрузить**
   - Отпустите **BOOT** когда появится "Connecting..."

### Шаг 6: Проверить загрузку

Откройте **Serial Monitor** (`Ctrl+Shift+M`):
- Скорость: **115200**

Должны увидеть:
```
=== SAMOVAR V2 STARTING ===
System not configured - starting AP mode
AP Name: SAMOVAR_A1B2C3D4
AP Password: 12345678
AP IP: 192.168.4.1
```

---

## 5️⃣ Web-настройка WiFi

### Шаг 1: Подключиться к WiFi точке ESP32

1. На телефоне/компьютере откройте список WiFi сетей
2. Найдите сеть `SAMOVAR_XXXXXXXX`
3. Подключитесь (пароль: `12345678`)

### Шаг 2: Открыть веб-интерфейс

1. Откройте браузер
2. Перейдите по адресу: **http://192.168.4.1**

Должна открыться страница настройки.

### Шаг 3: Заполнить форму

#### WiFi настройки:
- **SSID:** Название вашей домашней WiFi сети
- **Password:** Пароль от WiFi

#### Telegram Bot настройки:
- **Bot Token:** Вставьте токен из BotFather
```
  1234567890:ABCdefGHIjklMNOpqrsTUVwxyz123456789
```
- **Chat ID:** Вставьте ваш Chat ID
```
  123456789
```

### Шаг 4: Сохранить

1. Нажмите **"Сохранить и запустить"**
2. ESP32 сохранит настройки и перезагрузится
3. Через 5-10 секунд ESP32 подключится к вашей WiFi

### Шаг 5: Проверить подключение

В Serial Monitor должно появиться:
```
Connecting to WiFi: YourNetworkName
..........
WiFi Connected!
IP: 192.168.1.100
RSSI: -45 dBm
```

---

## 6️⃣ Проверка работы

### Шаг 1: Проверить Telegram бота

1. Откройте Telegram
2. Найдите вашего бота (например, @my_samovar_bot)
3. Должно прийти приветственное сообщение:
```
   🚀 SAMOVAR V2 ЗАПУЩЕН
   📅 14:30:25 07.01.2025
   🌐 IP: 192.168.1.100
   📶 RSSI: -45 dBm
   🌡️ Температура: 25.3°C

   Используйте /help для справки
```

### Шаг 2: Проверить команды

Отправьте:
```
/help
```

Должен прийти список команд.

Отправьте:
```
/?
```

Должен прийти статус системы.

### Шаг 3: Проверить дисплей

На дисплее должно отображаться:
```
[2][5].[3] [1][4].[3][0]
 Темп-ра    Время
 25.3°C     14:30
```

### Шаг 4: Протестировать управление
```
/alarm_on     → Разрешить зуммер
/zummer       → Включить зуммер (должен пищать)
/zummer       → Выключить зуммер
/valve_on     → Закрыть клапан (реле щелкнет)
/valve_off    → Открыть клапан
```

---

## 7️⃣ Устранение проблем

### Проблема: ESP32 не подключается к WiFi

**Решения:**
1. Проверьте правильность SSID и пароля
2. Убедитесь, что WiFi работает на **2.4 GHz** (ESP32 не поддерживает 5 GHz)
3. Проверьте расстояние до роутера
4. Попробуйте сбросить настройки (удержать BOOT 5 сек)

**Проверка в Serial Monitor:**
```
Connecting to WiFi: YourNetwork
....................
WiFi Connection FAILED!
```

### Проблема: Бот не отвечает

**Решения:**
1. Проверьте правильность Bot Token
2. Проверьте правильность Chat ID
3. Убедитесь, что ESP32 подключен к интернету (пингуется 8.8.8.8)
4. Проверьте, что бот не заблокирован

**Тест:**
- Откройте: `https://api.telegram.org/bot<YOUR_TOKEN>/getMe`
- Должен вернуть информацию о боте

### Проблема: Датчик DS18B20 не читается

**Serial Monitor показывает:**
```
CRITICAL: Sensor timeout!
```

**Решения:**
1. Проверьте подключение датчика
2. Проверьте резистор 4.7kΩ
3. Проверьте правильность пина (GPIO 2)
4. Попробуйте другой датчик

**Тест:**
```cpp
// Добавьте в loop():
Serial.println("Sensor address: " + String(ds.readAddress(), HEX));
```

### Проблема: Дисплей не работает

**Решения:**
1. Проверьте подключение (DIN=19, CLK=18, CS=5)
2. Проверьте питание (5V, не 3.3V!)
3. Проверьте, что модуль не поврежден

**Тест:**
```cpp
// В setup() после инициализации:
disp.clear();
disp.setCursor(0); disp.print(8);
disp.setCursor(1); disp.print(8);
disp.setCursor(2); disp.print(8);
disp.setCursor(3); disp.print(8);
disp.update();
delay(2000);
```

### Проблема: Зуммер не пищит

**Решения:**
1. Проверьте полярность (+ и -)
2. Проверьте транзистор (если используется)
3. Проверьте пин GPIO 33
4. Попробуйте команду `/zummer` после `/alarm_on`

### Проблема: Реле не щелкает

**Решения:**
1. Проверьте питание реле (5V)
2. Проверьте пин GPIO 26
3. Проверьте, что команда `/valve_on` отправлена
4. Измерьте напряжение на GPIO 26 мультиметром

---

## 🔄 Сброс к заводским настройкам

Если что-то пошло не так:

1. Удерживайте кнопку **BOOT** (GPIO 0) **5 секунд**
2. На дисплее появится: `rSEt`
3. ESP32 перезагрузится
4. Создастся точка доступа `SAMOVAR_XXXXXXXX`
5. Повторите настройку с **Шага 5**

---

## 📞 Поддержка

Если ничего не помогло:

1. Откройте [Issue на GitHub](https://github.com/your-username/samovar-esp32/issues)
2. Приложите:
   - Вывод Serial Monitor
   - Описание проблемы
   - Фото подключения (если актуально)

---

Удачной настройки! 🚀
